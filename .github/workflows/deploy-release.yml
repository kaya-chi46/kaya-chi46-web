name: Deploy to Firebase project Main (release)

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: 🚀 Deploy to Firebase Main
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build project
        run: npm run build

      # Google Cloud authentication
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KAYA_CHI46_WEB }}

      # Firebase CLI
      - name: Install Firebase CLI
        run: npm install -g firebase-tools@15.28.1

      # Deploy
      - name: Deploy to Firebase
        run: |
          firebase deploy --only hosting:release,functions --project kaya-chi46-web
