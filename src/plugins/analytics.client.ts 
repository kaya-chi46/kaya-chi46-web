import VueTag from 'vue-gtag'

export default defineNuxtPlugin((nuxtApp) => {
  const router = useRouter()

  nuxtApp.vueApp.use(
    VueGtag,
    {
      appName: 'kaya-chi46-web',
      pageTrackerScreenviewEnabled: true,
      config: { id: process.env.GTM_ID },
    },
    router
  )
})
